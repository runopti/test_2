#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv1, char *argv2, char *argv3)
{
	FILE *from, *to;
	char ch;

	if (argc != 3){
		if ((argc == 4) && (argv3 == "watch")
			watch();
		else
			printf("使用法: プログラム コピー元ファイル名 コピー先ファイル名 (watch)\n");
			exit(1)
	}

	
}
